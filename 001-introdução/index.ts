// no terminal npm install -g typescript(para instalar o typescript)
// no terminal tsc --init(para criar o json)
// fazer rodar com cntrl + alt + n

const pedido = (pedido: any) => {
    return `deu bom o seu pedido ${pedido}`

};

console.log(pedido(123));
